# 巡防百科新增内容设计：伤员救助与急救技能 + 老路径清理

日期：2026-08-07
状态：已确认

## 1. 背景与目标

巡防百科网站（`E:\xunfang-baike`）内容源自 Obsidian 知识库（`E:\frank知识库`，别名 frank知识库）中 `03_Knowledge/警务技能` 的原子化笔记。本次为网站新增两个方向的科普内容：

1. **伤员救助与急救技能**：巡逻警力遇路上人员受伤时的救助方法，科普简单包扎手法、AED 使用与常规救人手段，以科普学习为主。
2. **救助警情中的舆情管控**：作为救助类警情处置流程中的一个环节，短小精悍，讲清拉警戒线、架设围挡、围观劝离、法律宣传等实操动作。

同时清理 `data/content-inventory.json` 中指向旧路径（`F:\frank第二大脑\frank知识库`）的 source_files 引用。

## 2. 内容范围

**方案 A**（整体做方案，从网络素材到网站一步到位），精简版共 **6 篇文章**：

| # | 模块 | 文章标题 | 核心内容 |
|---|------|---------|---------|
| 1 | 警情 jingqing | 救助类警情处置流程 | 处置原则→**舆情管控**→现场评估→呼救→先期救助→现场保护→交接记录 |
| 2 | 训练 xunlian | 心肺复苏操作要点 | 适用判断、胸外按压、人工呼吸、按压呼吸比、注意事项 |
| 3 | 训练 xunlian | 止血、包扎与伤员搬运 | 直接压迫、加压包扎、止血带、绷带/三角巾包扎、搬运方法 |
| 4 | 装备 zhuangbei | 急救包组成与使用 | 常用急救物资清单、用途、检查维护 |
| 5 | 装备 zhuangbei | AED 使用方法 | 操作步骤（开机→贴片→听提示→电击）、注意事项 |
| 6 | 走访 zoufang | 场所急救设施与AED部署检查 | 检查要点、AED 配备、有效期检查、记录上报 |

不做：骨折固定、法规模块新增、勤务模块新增、走访急救培训普及。

舆情管控**不单独成文**，并入《救助类警情处置流程》一文，置于"处置原则"之后、"现场评估"之前。

## 3. Obsidian 笔记结构

按 `E:\frank知识库\CLAUDE.md`（知识库宪法）执行：

- **急救内容** → 新建子目录 `03_Knowledge/警务技能/伤员救助与急救/`，写成原子化笔记（每篇只阐述一个核心概念），预计 10-12 篇：CPR、AED、各类止血方式、包扎、搬运、急救包等。
- **救助处置/舆情管控** → 归入现有 `03_Knowledge/警务技能/快反处置/` 目录（属性匹配快反先期处置）。
- 每篇笔记：
  - 使用 `05_Templates` 中的对应模板
  - 添加 2-3 个精准标签（如 `#警务技能/急救`）
  - 添加至少 3 个双向链接 `[[相关笔记标题]]`
  - 末尾注明来源
- 主题笔记 ≥5 篇时创建 `MOC - 伤员救助与急救.md`。

## 4. 网站实施细节

- 每个模块按现有 HTML 结构（meta keywords/description、面包屑、速览卡片、章节）生成文章页面。
- 同步维护：
  - `data/content-inventory.json`：新增 6 篇文章条目，`source_files` 使用**当前正确路径** `E:\frank知识库\...`
  - `data/public-sources.json`：记录公开来源，可追溯
  - 搜索索引（`tools/build_search_index.py`）
- 舆情管控与处置流程属警务实务经验，公开出处有限：以公开可查资料为基础编写，来源台账如实标注"基于公开资料整理"，不虚构出处。急救科普使用权威公开源（红十字/急救指南/医院科普等）。

## 5. 老路径清理

- 现状：`data/content-inventory.json` 中 165 处 `source_files` 指向 `F:\frank第二大脑\frank知识库\...`
- 已验证：全部 165 处替换前缀 `F:\frank第二大脑\frank知识库` → `E:\frank知识库` 后，目标文件均存在（0 处断裂）
- 处理方式：前缀整体替换，写入设计文档后再统一执行

## 6. 执行流程

1. 网络搜索素材，按子主题整理成文案
2. 按 Obsidian 宪法写成原子化笔记（急救→新建目录，处置/舆情→快反处置）
3. 从笔记转写网站 HTML（6 篇）
4. 同步 content-inventory.json（新增条目 + 老路径清理）、public-sources.json
5. 重建搜索索引、检查站内链接、校验来源台账
6. 运行验证与测试

## 7. 验证

按 README 完整验证：
- `python -m unittest discover -s tests -p "test_*.py" -v`
- `node --test tests/auth_core.test.js`
- `python tools/build_search_index.py --check`
- `python tools/check_site_links.py`
- `python tools/public_source_index.py check`

## 8. 风险与注意

- **Python 未安装**：当前环境（Git Bash + Windows）中无可用 Python（仅有商店占位符），Node.js v24.18.1 可用。README 要求的 Python 验证命令可能需要：安装 Python 3.12，或用 Node 方案替代。实施时优先与用户确认。
- 只改该改的内容：不触碰与本次无关的文章、格式与备注。
- 不虚构来源：处置/舆情类内容如实标注"基于公开资料整理"。
